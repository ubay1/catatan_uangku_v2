export interface IPropsButton {
  title: string;
  mode: any;
  uppercase?: boolean;
  color?: any;
  disabled?: boolean;
  theme?: object,
  action?: any;
}
export interface IPropsButtonWithIcon {
  title: string;
  mode: any;
  icon: string;
  uppercase?: boolean;
  color?: any;
  disabled?: boolean;
  theme?: object,
  action?: any;
}
